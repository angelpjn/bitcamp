/*
7.9 순환 배열 : CircularArray 클래스를 구현하라.
이 클래스는 배열과 비슷한 자료구조이지만 효과적으로 순환(rotate)이 가능해야 한다.
클래스는 가능하면 제네릭 타입(generic type)혹은 템플릿(template)으로 구현되는 게 좋고,
for (Obj o : circularArray)와 같이 푝준 표기법으로 순회(iterate)가 가능해야 한다. 

*/

package solve_original.solve_interview.ch07;
